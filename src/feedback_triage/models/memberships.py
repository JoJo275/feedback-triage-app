"""SQLModel ORM model for the ``memberships`` table.

**Stub.** Filled in by PR 1.3b alongside Migration A. Maps a ``user`` to
a ``workspace`` with a ``workspace_role_enum`` value (owner / admin /
member); see ADR 060 and ``docs/project/spec/v2/auth.md``.
"""

from __future__ import annotations
