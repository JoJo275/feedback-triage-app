"""SQLModel ORM model for the ``auth_rate_limits`` table.

**Stub.** Filled in by PR 1.3b alongside Migration A. Backs the
sign-in / sign-up / token-mint rate-limit counters that the auth
module (PR 1.4) consults; see ``docs/project/spec/v2/auth.md``.
"""

from __future__ import annotations
