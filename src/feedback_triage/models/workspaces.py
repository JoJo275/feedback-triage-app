"""SQLModel ORM model for the ``workspaces`` table.

**Stub.** Filled in by PR 1.3b alongside Migration A. The workspace is
the v2 tenancy boundary (ADR 060); the synthetic ``signalnest-legacy``
workspace receives every v1.0 ``feedback_item`` row during Migration B
(ADR 062).
"""

from __future__ import annotations
